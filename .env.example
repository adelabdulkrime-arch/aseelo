# ============================================================
# ASEELO - Environment configuration example
# Copy to .env and adjust. NEVER commit a real .env file.
# ============================================================

# ---------- Core ----------
APP_ENV=development
LOG_LEVEL=INFO

# ---------- Database ----------
# Inside docker compose the host is the service name "postgres".
DATABASE_URL=postgresql+psycopg://aseelo:aseelo@postgres:5432/aseelo
POSTGRES_USER=aseelo
POSTGRES_PASSWORD=aseelo
POSTGRES_DB=aseelo

# ---------- Queue ----------
REDIS_URL=redis://redis:6379/0

# ---------- Security ----------
# Generate with: python -c "import secrets;print(secrets.token_urlsafe(48))"
JWT_SECRET=change-me-in-production-please-use-a-long-random-value
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
# Comma separated list of allowed browser origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ---------- Storage ----------
# local | s3
STORAGE_PROVIDER=local
STORAGE_PATH=/data/storage
# Public base URL used to build media links served by the backend
PUBLIC_MEDIA_BASE_URL=http://localhost:8000/media

# S3 / R2 / MinIO (only used when STORAGE_PROVIDER=s3)
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=aseelo
S3_REGION=auto
S3_PUBLIC_BASE_URL=

# ---------- Uploads ----------
# 512 MB
MAX_UPLOAD_SIZE=536870912
MAX_LOGO_SIZE=5242880
MAX_VIDEO_DURATION_SECONDS=180
MIN_VIDEO_DURATION_SECONDS=1

# ---------- FFmpeg ----------
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe
OUTPUT_WIDTH=1080
OUTPUT_HEIGHT=1920
OUTPUT_FPS=30
OUTPUT_CRF=20
OUTPUT_PRESET=medium
OUTPUT_AUDIO_BITRATE=128k

# ---------- Rate limiting ----------
RATE_LIMIT_ENABLED=true
UPLOAD_RATE_LIMIT=30/hour

# ---------- Frontend ----------
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=ASEELO

# ---------- Guest sessions ----------
# The only way into the app - there is no login. ON by default. Each guest can
# queue a render, and one render saturates a single core - turn this off as an
# emergency brake if the host cannot keep up, not because it is optional.
GUEST_SESSIONS_ENABLED=true
GUEST_RATE_LIMIT=2/hour
# How long a guest's work survives before scripts.prune_guests reclaims it.
GUEST_RETENTION_DAYS=7
# Guests only; registered users keep MAX_VIDEO_DURATION_SECONDS.
# Measured: ~17.6x realtime on one saturated core, so 20s is ~6 min of render.
GUEST_MAX_VIDEO_DURATION_SECONDS=20
